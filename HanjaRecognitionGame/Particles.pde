class Particles{
  
  //Particles class takes a RectWalldoor class, which has combined with the TextHandler output. 
  //Basically it will have text made into an image  (PImage or PFont)
  
Particles(RectWalldoor pDoor){
  // If constructor is called then you're already sure that a door has been opened, so you called the constructor
  
}

}
